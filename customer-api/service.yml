apiVersion: v1
kind: Service
metadata:
  name: customer-api-service
  namespace: default
spec:
  selector:
    app: customer-api
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30000
